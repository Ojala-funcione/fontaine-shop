import React from 'react';
import { Box } from '@chakra-ui/react';
import ScrollToTopButton from '@common/Buttons/ScrollToTopButton';

const Store = () => (
  <>
    <ScrollToTopButton />
    <Box minH="300vh">Todo el contenido de Store aca</Box>
  </>
);

export default Store;
