import React from 'react';
import { Box } from '@chakra-ui/react';
import ScrollToTopButton from '@common/Buttons/ScrollToTopButton';

const adminDashboard = () => (
  <>
    <ScrollToTopButton />
    <Box minH="100vh">soy el dashhhh...</Box>
  </>
);

export default adminDashboard;
